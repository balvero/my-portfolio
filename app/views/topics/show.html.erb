<div class="container-fluid bd">
  <div class="row">
    <div class="col-md-9 m-auto">
      <h2><%= @topic.title %></h2>

      <div class="row">
        <%= paginate @blogs %>
      </div>

      <%= render @blogs %>

      <div class="row">
        <%= paginate @blogs %>
      </div>
    </div>
    <div class="col-md-3">
      <%= render 'blogs/sidebar' %>
    </div>
  </div>
</div>