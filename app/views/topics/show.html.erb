<div class="col-md-8 m-auto">
  <h2><%= @topic.title %></h2>

  <div class="row">
    <%= paginate @blogs %>
  </div>

  <%= render @blogs %>

  <div class="row">
    <%= paginate @blogs %>
  </div>
</div>